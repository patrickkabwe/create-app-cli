
export const {{lowerCase moduleName}}TypeDefs = `#graphql
  type {{titleCase moduleName}} {
    id: String
  }

  input {{titleCase moduleName}}Input {
    id: String
  }

  type Query {
    get{{ titleCase moduleName }}(id: String!): {{titleCase moduleName}}
    get{{ pluralize (titleCase  moduleName)}}: [{{titleCase moduleName}}]
  }

  type Mutation {
    create{{titleCase moduleName}}(input: {{titleCase moduleName}}Input!): {{titleCase moduleName}}
    update{{titleCase moduleName}}(id: String!, input: {{titleCase moduleName}}Input!): {{titleCase moduleName}}
    delete{{titleCase moduleName}}(id: String!): {{titleCase moduleName}}
  }
`;
